 @using Microsoft.AspNetCore.Http
@inject IHttpContextAccessor HttpContextAccessor
@{
    var email = HttpContextAccessor.HttpContext.Session.GetString("_email");
}

<div class="main-sidebar sidebar-style-2">
        <aside id="sidebar-wrapper">
          <div class="sidebar-brand">
            <a><img alt="image" src="" class="header-logo" /> 
            </a>
          </div>
        @if (!string.IsNullOrEmpty(email))
        {
            <ul class="sidebar-menu">
                <li class="menu-header">Main</li>
            <li class="dropdown active">
              <a href="@Url.Action("Index", "Home")" class="nav-link"><i data-feather="monitor"></i><span>Dashboard</span></a>
            </li>           
            
            <li class="dropdown">
              <a href="#" class="menu-toggle nav-link has-dropdown"><i data-feather="command"></i><span>Orders</span></a>
              <ul class="dropdown-menu">
                        <li><a class="nav-link" href="@Url.Action("CreateOrder", "Home")">New Order</a></li>
                <li><a class="nav-link" href="@Url.Action("Index", "Home")">View Orders</a></li>
              </ul>
            </li>
            
          </ul>
        }
        </aside>
        </div>